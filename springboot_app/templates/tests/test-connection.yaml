apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot_app.fullname" . }}-test-connection"
  labels:
    {{- include "springboot_app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springboot_app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
